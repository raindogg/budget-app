<h1><%= @month.name %>, <%= @month.year %></h1>

<h2>Add an expense:</h2>

<%= form_tag '/entries', method: :post do %>
  <%= label_tag :name %>
  <%= text_field_tag :name, nil, placeholder: "Name" %>

  <%= label_tag :amount %>
  <%= text_field_tag :amount, nil, placeholder: "Amount" %>

  <%= label_tag :category %>
  <%= collection_select :category, :category_id, Category.all, :id, :name %>

  <%= hidden_field_tag :month_id, @month.id %>

  <%= submit_tag "Submit" %>

<% end %>

<h2>Totals:</h2>
<h3>Savings</h3>
<p><%= number_to_currency(@month.all_savings) %></p>

<h3>Bills</h3>
<p><%= number_to_currency(@month.all_bills) %></p>

<h3>Groceries</h3>
<p><%= number_to_currency(@month.all_groceries) %></p>

<h3>Intoxicants</h3>
<p><%= number_to_currency(@month.all_intoxicants) %></p>

<h3>Transportation</h3>
<p><%= number_to_currency(@month.all_transportation) %></p>

<h3>Pets</h3>
<p><%= number_to_currency(@month.all_pets) %></p>

<h3>Restaurants</h3>
<p><%= number_to_currency(@month.all_restaurants) %></p>

<h3>Bars, Movies, Entertainment</h3>
<p><%= number_to_currency(@month.all_bme) %></p>

<h3>Miscellaneous</h3>
<p><%= number_to_currency(@month.all_misc) %></p>

<h2>All Expenses: <%= number_to_currency(@month.all_expenses) %></h2>

<h2>Bills</h2>
<ul>
  <% @bills.each do |bill| %>
    <li><b><%= bill.name  %></b>: <%= number_to_currency(bill.amount) %></li>
  <% end %>
</ul>

<h2>Groceries</h2>
<ul>
  <% @groceries.each do |grocery| %>
    <li><b><%= grocery.name  %></b>: <%= number_to_currency(grocery.amount) %></li>
  <% end %>
</ul>

<h2>Intoxicants</h2>
<ul>
  <% @intox.each do |entry| %>
    <li><b><%= entry.name  %></b>: <%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

<h2>Transportation</h2>
<ul>
  <% @transport.each do |entry| %>
    <li><b><%= entry.name  %></b>: <%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

<h2>Pets</h2>
<ul>
  <% @pets.each do |entry| %>
    <li><b><%= entry.name  %></b>: <%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

<h2>Restaurants</h2>
<ul>
  <% @restaurants.each do |entry| %>
    <li><b><%= entry.name  %></b>: <%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

<h2>Bars, Movies, Entertainment</h2>
<ul>
  <% @bme.each do |entry| %>
    <li><b><%= entry.name  %></b>: <%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

<h2>Miscellaneous</h2>
<ul>
  <% @misc.each do |entry| %>
    <li><b><%= entry.name  %></b>: <%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

