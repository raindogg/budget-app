<h1><%= @month.name %>, <%= @month.year %></h1>
<h2>Status </h2>
<h3>Total income: <%= number_to_currency(@month.total(@income)) %></h3>
<h3>Total expenses: <%= number_to_currency(@month.all_expenses) %></h3>
<h3>Total remaining: <%= number_to_currency(@balance) %></h3>

<h2>Add an expense:</h2>

<%= form_tag '/entries', method: :post do %>
  <%= label_tag :name %>
  <%= text_field_tag :name, nil, placeholder: "Name" %>

  <%= label_tag :amount %>
  <%= text_field_tag :amount, nil, placeholder: "Amount" %>

  <%= label_tag :category %>
  <%= collection_select :category, :category_id, Category.all, :id, :name %>

  <%= hidden_field_tag :month_id, @month.id %>

  <%= submit_tag "Submit" %>

<% end %>

<h2>Totals:</h2>
<h3>Savings</h3>
<p><%= number_to_currency(@month.total(@savings)) %></p>

<h3>Bills</h3>
<p><%= number_to_currency(@month.total(@bills)) %></p>

<h3>Groceries</h3>
<p><%= number_to_currency(@month.total(@groceries)) %></p>

<h3>Intoxicants</h3>
<p><%= number_to_currency(@month.total(@intox)) %></p>

<h3>Transportation</h3>
<p><%= number_to_currency(@month.total(@transport)) %></p>

<h3>Pets</h3>
<p><%= number_to_currency(@month.total(@pets)) %></p>

<h3>Restaurants</h3>
<p><%= number_to_currency(@month.total(@restaurants)) %></p>

<h3>Bars, Movies, Entertainment</h3>
<p><%= number_to_currency(@month.total(@bme)) %></p>

<h3>Miscellaneous</h3>
<p><%= number_to_currency(@month.total(@misc)) %></p>

<h2>Savings</h2>
<ul>
  <% @savings.each do |entry| %>
  <li><b><%= entry.id %> <%= entry.name %></b>:<%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

<h2>Income</h2>
<ul>
  <% @income.each do |entry| %>
  <li><b><%= entry.id %> <%= entry.name %></b>:<%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>
<h2>Bills</h2>
<ul>
  <% @bills.each do |bill| %>
    <li><b><%= bill.id %> <%= bill.name  %></b>: <%= number_to_currency(bill.amount) %></li>
  <% end %>
</ul>

<h2>Groceries</h2>
<ul>
  <% @groceries.each do |grocery| %>
    <li><b><%= grocery.name  %></b>: <%= number_to_currency(grocery.amount) %></li>
  <% end %>
</ul>

<h2>Intoxicants</h2>
<ul>
  <% @intox.each do |entry| %>
    <li><b><%= entry.name  %></b>: <%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

<h2>Transportation</h2>
<ul>
  <% @transport.each do |entry| %>
    <li><b><%= entry.name  %></b>: <%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

<h2>Pets</h2>
<ul>
  <% @pets.each do |entry| %>
    <li><b><%= entry.name  %></b>: <%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

<h2>Restaurants</h2>
<ul>
  <% @restaurants.each do |entry| %>
    <li><b><%= entry.name  %></b>: <%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

<h2>Bars, Movies, Entertainment</h2>
<ul>
  <% @bme.each do |entry| %>
    <li><b><%= entry.name  %></b>: <%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

<h2>Miscellaneous</h2>
<ul>
  <% @misc.each do |entry| %>
    <li><b><%= entry.name  %></b>: <%= number_to_currency(entry.amount) %></li>
  <% end %>
</ul>

